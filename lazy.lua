return {
  "bkoropoff/clipipe",
  opts = {
    path = nil,
    keep_line_endings = false,
    enable = true,
    start_timeout = 5000,
    timeout = 500,
    interval = 50,
    build = true,
    download = true
  }
}
